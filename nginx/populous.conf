upstream flattening_backend {
    server 127.0.0.1:4321;
}

# HTTP → HTTPS redirect for all domains
server {
    listen 80;
    server_name the-flattening.com www.the-flattening.com populousreborn.com www.populousreborn.com;
    return 301 https://the-flattening.com$request_uri;
}

# www.the-flattening.com → the-flattening.com
server {
    listen 443 ssl http2;
    server_name www.the-flattening.com;

    ssl_certificate     /etc/letsencrypt/live/the-flattening.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/the-flattening.com/privkey.pem;

    return 301 https://the-flattening.com$request_uri;
}

# populousreborn.com → the-flattening.com
server {
    listen 443 ssl http2;
    server_name populousreborn.com www.populousreborn.com;

    ssl_certificate     /etc/letsencrypt/live/populousreborn.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/populousreborn.com/privkey.pem;

    return 301 https://the-flattening.com$request_uri;
}

# Main site
server {
    listen 443 ssl http2;
    server_name the-flattening.com;

    ssl_certificate     /etc/letsencrypt/live/the-flattening.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/the-flattening.com/privkey.pem;

    # WebSocket support
    location / {
        proxy_pass http://flattening_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket timeouts — keep connections alive
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }
}
